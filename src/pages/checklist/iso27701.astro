---
import Base from '../../layouts/BaseLayout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';

const base = import.meta.env.BASE_URL || '/';
const STANDARD = 'ISO 27701 (Privacy)';

/**
 * Acronym glossary (single source of truth)
 * UI will DISPLAY the plain-English term; acronym is only in the tooltip.
 */
const GLOSSARY = {
  PIMS: { term: 'Privacy Information Management System', desc: 'Your organisation’s privacy governance, processes and evidence—how you manage personal information end-to-end.' },
  PII:  { term: 'Personal information (identifiable data)', desc: 'Information that can identify a person (directly or indirectly), such as name, email, phone, Medicare number, IP address, or patient ID.' },
  DPO:  { term: 'Privacy lead (DPO equivalent)', desc: 'A designated privacy lead responsible for privacy oversight, advice, monitoring and reporting.' },
  PO:   { term: 'Privacy Officer', desc: 'The accountable person/team who runs privacy operations day-to-day.' },
  ROPA: { term: 'Processing register (ROPA)', desc: 'A register of what personal data you process, why, where it flows, who you share it with, and how long you keep it.' },
  DPIA: { term: 'High-risk assessment (DPIA)', desc: 'A structured assessment used when processing is likely to create high privacy risk (e.g., new tech, large-scale sensitive data).' },
  SRR:  { term: 'Privacy request from individuals', desc: 'Requests from individuals to access, correct, delete, export or object to how their data is used.' },
  KPI:  { term: 'Performance target (KPI)', desc: 'A measurable target (e.g., “respond to privacy requests within 20 days”).' },
  DPA:  { term: 'Vendor privacy contract (DPA)', desc: 'Contract terms that define how a vendor must handle personal data and what controls they must apply.' },
  SCC:  { term: 'Standard transfer clauses (SCCs)', desc: 'Contract terms commonly used to support international data transfers in certain jurisdictions.' },
  TIA:  { term: 'Overseas transfer risk check (TIA)', desc: 'An assessment of transfer risks and safeguards when personal data is sent overseas.' },
  M365: { term: 'Microsoft 365 evidence location', desc: 'Where evidence and records are stored (e.g., SharePoint) and controlled (versioning/retention).' },
  Purview:{ term:'Microsoft Purview', desc:'Information protection, data governance and compliance tooling (labels, retention, DLP, eDiscovery).' },
  Entra: { term:'Microsoft Entra ID', desc:'Identity and access management used to control access and enforce sign-in policies.' },
  Intune:{ term:'Microsoft Intune', desc:'Endpoint/device management used to enforce security baselines and configuration policies.' },
};

const GROUPS = [
  { key:'gov', title:'Leadership & Ownership', blurb:'Define scope, assign ownership, keep policies current, track improvement.' },
  { key:'map', title:'Know Your Data',         blurb:'Maintain your data register and understand where personal data flows.' },
  { key:'ctrl',title:'Everyday Privacy Controls', blurb:'Minimise data, control access, manage consent, handle requests, train people.' },
  { key:'tp',  title:'Vendors & Overseas Data', blurb:'Vendor due diligence, contracts, and managing overseas transfers.' },
  { key:'ev',  title:'Audit-Ready Evidence',   blurb:'Store evidence, track metrics, test controls internally, and review with leadership.' },
];

const QUESTIONS = [
  // Leadership & Ownership
  {
    id:'pims-scope', group:'gov',
    title:'We’ve clearly defined what personal information we manage and where the privacy program applies.',
    acronyms:['PIMS','PII'],
    why:'We’re looking for a clear scope so privacy obligations don’t fall through gaps (systems, regions, business units, products).',
    examples:[
      'A written scope statement covering products, systems, locations and key data types.',
      'A clear “in scope vs out of scope” list (with justification).',
      'Defined boundaries for subsidiaries/tenants/regions (e.g., AU only vs AU + EU).'
    ],
    tip:'Partial usually means “we have a rough idea, but it isn’t documented or consistently applied.”'
  },
  {
    id:'roles-dpo', group:'gov',
    title:'Privacy ownership is assigned and people know who decides, approves and responds.',
    acronyms:['DPO','PO','DPIA'],
    why:'We’re looking for accountability: who owns privacy risk, who runs requests, who signs off assessments, and who reports to leadership.',
    examples:[
      'Named privacy owner + backup, with responsibilities documented.',
      'Clear escalation path for complaints and incidents.',
      'A simple RACI for privacy operations (requests, incidents, assessments).'
    ]
  },
  {
    id:'policies', group:'gov',
    title:'Our privacy policies and notices are current, approved, and used in practice.',
    acronyms:['SRR'],
    why:'We’re looking for “rules people follow”: notices, retention rules, incident handling, and request handling.',
    examples:[
      'Customer privacy notice reviewed in the last 12 months.',
      'Internal procedure for handling access/deletion requests.',
      'Retention rules documented (what is kept, why, and for how long).'
    ]
  },
  {
    id:'objectives', group:'gov',
    title:'We track a small set of privacy targets so leadership can see improvement over time.',
    acronyms:['KPI','SRR'],
    why:'We’re looking for measurable privacy performance (request turnaround, training completion, incidents, assessments completed).',
    examples:[
      'Targets for request response time and backlog.',
      'Regular reporting to leadership on trends and hotspots.',
      'Training completion rate by role.'
    ]
  },

  // Know Your Data
  {
    id:'ropa', group:'map',
    title:'We keep an up-to-date register of what personal data we process and why.',
    acronyms:['ROPA','PII'],
    why:'We’re looking for a defensible register that connects processing to purpose, sharing, retention and safeguards.',
    examples:[
      'Register entries for core processes (support, billing, product telemetry).',
      'Each entry includes purpose, data categories, systems, vendors and retention.',
      'A review cadence (e.g., quarterly or on major changes).'
    ]
  },
  {
    id:'pii-flows', group:'map',
    title:'We can explain where personal data comes from, where it goes, and who we share it with.',
    acronyms:['PII'],
    why:'We’re looking for visibility of flows so you can answer due diligence and respond to incidents quickly.',
    examples:[
      'A simple diagram: collection → app → database → analytics → support tooling.',
      'List of vendors that receive personal data.',
      'Identification of any overseas access/hosting (if applicable).'
    ]
  },
  {
    id:'dpia', group:'map',
    title:'We assess high-risk changes before launch (new features, new vendors, new uses of data).',
    acronyms:['DPIA'],
    why:'We’re looking for a repeatable method to identify high privacy risk early and document decisions/safeguards.',
    examples:[
      'Clear “when to assess” triggers (e.g., sensitive data, profiling, large-scale monitoring).',
      'Template with risk scoring and sign-off.',
      'Evidence of completed assessments for recent major changes.'
    ]
  },

  // Everyday Privacy Controls
  {
    id:'controls', group:'ctrl',
    title:'We collect only what we need, keep it only as long as needed, and restrict access appropriately.',
    acronyms:['PII'],
    why:'We’re looking for privacy-by-design in practice: minimisation, retention, access control, and secure handling.',
    examples:[
      'Default retention periods with deletion where possible.',
      'Access based on role (e.g., support access is time-bound and logged).',
      'Test data is separated/sanitised from production data.'
    ]
  },
  {
    id:'consent', group:'ctrl',
    title:'Where consent is required, we can prove it and prove when it changed or was withdrawn.',
    acronyms:[],
    why:'We’re looking for auditable records: what was agreed, when, how captured, and how changes are honoured.',
    examples:[
      'Consent logs linked to a user/customer record.',
      'Preference centre with change history.',
      'Downstream tools updated to honour opt-outs.'
    ]
  },
  {
    id:'rights', group:'ctrl',
    title:'We can handle privacy requests (access/correction/deletion) within a defined timeframe.',
    acronyms:['SRR'],
    why:'We’re looking for an end-to-end workflow: intake, verification, triage, completion, and response evidence.',
    examples:[
      'Ticket workflow with timestamps and assigned owner.',
      'Identity verification step before disclosure.',
      'Response templates and escalation path.'
    ]
  },
  {
    id:'training', group:'ctrl',
    title:'Privacy training is assigned by role and we can show who completed it.',
    acronyms:[],
    why:'We’re looking for competence evidence—especially for staff handling personal data, requests, or vendors.',
    examples:[
      'Annual refresher + onboarding module.',
      'Extra training for support/engineering/leaders.',
      'Completion reports and follow-up for overdue training.'
    ]
  },

  // Vendors & Overseas Data
  {
    id:'processors', group:'tp',
    title:'We assess privacy risk in vendors and contracts define privacy expectations clearly.',
    acronyms:['DPA','SCC'],
    why:'We’re looking for due diligence + contract coverage + ongoing control when vendors change.',
    examples:[
      'Vendor risk assessment: what data they get + controls they have.',
      'Vendor privacy contract terms in place for data-handling vendors.',
      'Sub-vendor register and notification/review process.'
    ]
  },
  {
    id:'xfer', group:'tp',
    title:'If personal data goes overseas, we assess and document transfer risks and safeguards.',
    acronyms:['SCC','TIA'],
    why:'We’re looking for evidence that overseas transfers are understood, approved and reviewed over time.',
    examples:[
      'Register of overseas hosting/support access locations.',
      'Transfer risk assessment documented for key transfers.',
      'Reassessment trigger when vendors change regions/sub-vendors.'
    ]
  },

  // Audit-Ready Evidence
  {
    id:'evidence', group:'ev',
    title:'Privacy evidence is stored in one place with versioning so we can answer audits quickly.',
    acronyms:['M365','Purview','Entra','Intune'],
    why:'We’re looking for audit-ready evidence: policies, registers, assessments, request logs, vendor docs and approvals.',
    examples:[
      'SharePoint library with structure and naming standards.',
      'Version control enabled and ownership defined.',
      'Retention labels or retention policy applied.'
    ]
  },
  {
    id:'metrics', group:'ev',
    title:'We report privacy activity so we can spot trends and reduce repeat issues.',
    acronyms:['SRR'],
    why:'We’re looking for visibility: request volumes, backlog, incidents, assessments, vendor reviews and training.',
    examples:[
      'Monthly dashboard for request volumes and completion time.',
      'Trend reporting on complaints/root causes.',
      'Training completion and overdue metrics.'
    ]
  },
  {
    id:'iaudit', group:'ev',
    title:'We check privacy controls internally and track fixes through to completion.',
    acronyms:[],
    why:'We’re looking for internal assurance: findings, corrective actions, owners and closure evidence.',
    examples:[
      'Internal check plan and report.',
      'Corrective action register with dates and owners.',
      'Verification that actions were completed.'
    ]
  },
  {
    id:'review', group:'ev',
    title:'Leadership reviews privacy performance and decides priorities and resourcing.',
    acronyms:['PIMS'],
    why:'We’re looking for management oversight: risks, incidents, complaints, metrics and improvement actions.',
    examples:[
      'Management review minutes (quarterly/biannual).',
      'Decisions recorded (e.g., fund remediation, fix backlog).',
      'Actions assigned and tracked to closure.'
    ]
  },
];

const CHECKLIST_DATA = { STANDARD, GROUPS, QUESTIONS, GLOSSARY };
---

<Base
  title="ISO 27701 Free Privacy Readiness Checklist"
  description="Free ISO 27701 privacy readiness checklist for privacy officers, CISOs and compliance leaders. Assess privacy maturity, see your readiness score and download a PDF roadmap to certification."
>
  <Header />
  <img id="pdfLogo" src={`${base}assets/logo.png`} alt="" style="display:none" width="120" height="120" />

  <section class="container section">
    <h1>ISO 27701 privacy readiness checklist (free)</h1>
    <p class="muted" style="max-width:820px">
      Answer a short set of business-friendly questions to see how mature your privacy practices are.
      You’ll get a readiness score and a PDF report explaining gaps, risks and next steps.
    </p>

    <div class="info-cards">
      <div class="card soft">
        <h4>Objective</h4>
        <p class="small">Understand how close you are to a consistent, audit-ready privacy program aligned to ISO 27701.</p>
      </div>
      <div class="card soft">
        <h4>Scoring</h4>
        <p class="small"><strong>Yes / Partial / No</strong>. Your progress and score update automatically.</p>
      </div>
      <div class="card soft">
        <h4>Output</h4>
        <p class="small">Download a branded PDF with domain breakdown, top gaps and practical next steps.</p>
      </div>
    </div>
  </section>

  <section class="container section">
    <div class="tabs">
      <a href={`${base}checklist/iso27001`} class="pill">ISO 27001</a>
      <a href={`${base}checklist/iso27701`} class="pill" aria-current="page">ISO 27701</a>
      <a href={`${base}checklist/iso42001`} class="pill">ISO 42001</a>
      <a href={`${base}checklist/essential-eight`} class="pill">Essential Eight</a>
      <a href={`${base}checklist/soc2`} class="pill">SOC 2</a>
      <a href={`${base}checklist/disp-ism-irap`} class="pill">DISP / ISM / IRAP</a>
    </div>

    <div class="progress">
      <div id="bar" class="progress-bar"></div>
    </div>

    <div class="card">
      <div class="stickyScore">
        <div class="stickyLeft">
          <div class="stickyTitle">Your readiness</div>
          <div class="stickyMeta muted small" id="answeredMeta">0/0 answered</div>
        </div>
        <div class="stickyRight">
          <div id="scoreNum" class="scoreNum">0%</div>
          <div class="scoreText">
            <div><strong id="scoreLabel">Not started</strong></div>
            <div id="scoreHint" class="muted small">Answer the questions to see your readiness.</div>
          </div>
        </div>
      </div>

      <div id="form" class="form-grid">
        {GROUPS.map((grp) => (
          <section class="group">
            <div class="groupHead">
              <div>
                <h3 style="margin:0">{grp.title}</h3>
                <p class="muted small" style="margin:.2rem 0 0">{grp.blurb}</p>
              </div>
              <div id={`g-${grp.key}`} class="muted small">0/0 answered</div>
            </div>

            <div class="group-fields">
              {QUESTIONS.filter(q => q.group === grp.key).map((q) => (
                <div class="field" data-id={q.id} data-group={q.group}>
                  <label class="qTitle">{q.title}</label>

                  <div class="qMeta">
                    <a href="#" class="detailsToggle" data-toggle={`ex-${q.id}`}>What we’re looking for (examples)</a>

                    {q.acronyms?.length ? (
                      <span class="qChips">
                        {q.acronyms.map(a => (
                          <span
                            class="chip"
                            title={`${a} — ${GLOSSARY[a]?.term || a}: ${GLOSSARY[a]?.desc || ''}`}
                          >
                            {GLOSSARY[a]?.term || a}
                          </span>
                        ))}
                      </span>
                    ) : null}
                  </div>

                  <div id={`ex-${q.id}`} class="explain" style="display:none;">
                    <p class="small" style="margin:0 0 6px;">
                      <strong>What good looks like:</strong> {q.why}
                    </p>
                    <ul class="small" style="margin:0; padding-left:18px;">
                      {q.examples?.map(ex => <li>{ex}</li>)}
                    </ul>
                    {q.tip ? <p class="small muted" style="margin:8px 0 0;"><strong>Tip:</strong> {q.tip}</p> : null}
                  </div>

                  <!-- IMPORTANT: no `.pill` class here to avoid collisions -->
                  <div class="opts">
                    <button class="optBtn" data-v="2" type="button">Yes</button>
                    <button class="optBtn" data-v="1" type="button">Partial</button>
                    <button class="optBtn" data-v="0" type="button">No</button>
                  </div>
                </div>
              ))}
            </div>
          </section>
        ))}
      </div>

      <div class="actions">
        <button class="btn btn-ghost" id="resetBtn" type="button">Reset</button>
        <button class="btn btn-primary" id="pdfBtn" type="button">Download PDF</button>
      </div>
    </div>

    <div class="card soft" style="margin-top:14px">
      <h3 style="margin:0 0 6px">Acronym guide (plain English)</h3>
      <p class="muted small" style="margin:0 0 10px">
        The chips above show the plain-English meaning; hover to see the acronym reference.
      </p>
      <div class="glossaryGrid">
        {Object.entries(GLOSSARY).map(([k,v]) => (
          <div class="glossaryItem">
            <div class="glossKey">{k}</div>
            <div class="glossBody">
              <div class="glossTerm">{v.term}</div>
              <div class="muted small">{v.desc}</div>
            </div>
          </div>
        ))}
      </div>
    </div>

  </section>

  <Footer />

  <script src="https://cdn.jsdelivr.net/npm/jspdf@2.5.1/dist/jspdf.umd.min.js" defer></script>

  <script type="application/json" id="checklistData">
    {JSON.stringify(CHECKLIST_DATA)}
  </script>

  <script is:inline>
(() => {
  'use strict';

  const API_URL = "https://zzb9g575zh.execute-api.ap-southeast-2.amazonaws.com/Prod/contact-us";

  const dataEl = document.getElementById('checklistData');
  const DATA = dataEl ? JSON.parse(dataEl.textContent) : {};
  const STANDARD = DATA.STANDARD || "ISO 27701 (Privacy)";
  const GROUPS = Array.isArray(DATA.GROUPS) ? DATA.GROUPS : [];
  const QUESTIONS = Array.isArray(DATA.QUESTIONS) ? DATA.QUESTIONS : [];

  (document.readyState === 'loading') ? document.addEventListener('DOMContentLoaded', setup) : setup();

  function setup(){
    const form         = document.getElementById('form');
    const bar          = document.getElementById('bar');
    const scoreNum     = document.getElementById('scoreNum');
    const scoreLabel   = document.getElementById('scoreLabel');
    const scoreHint    = document.getElementById('scoreHint');
    const answeredMeta = document.getElementById('answeredMeta');
    const resetBtn     = document.getElementById('resetBtn');
    const pdfBtn       = document.getElementById('pdfBtn');

    const modal        = document.getElementById('leadModal');
    const leadForm     = document.getElementById('leadForm');
    const cancelLead   = document.getElementById('cancelLead');
    const nameEl       = document.getElementById('name');
    const emailEl      = document.getElementById('email');

    const state = Object.fromEntries(QUESTIONS.map(q => [q.id, null]));

    function refreshGroupCounters(){
      let totalAnswered = 0;
      GROUPS.forEach(grp => {
        const total = QUESTIONS.filter(q=>q.group===grp.key).length;
        const ansd  = QUESTIONS.filter(q=>q.group===grp.key).map(q=>state[q.id]).filter(v=>v!==null).length;
        totalAnswered += ansd;
        const el = document.getElementById('g-'+grp.key);
        if(el) el.textContent = `${ansd}/${total} answered`;
      });
      if (answeredMeta) answeredMeta.textContent = `${totalAnswered}/${QUESTIONS.length} answered`;
    }

    // Event delegation for toggles + answers
    form?.addEventListener('click', (e) => {
      // Toggle details
      const toggle = e.target.closest('[data-toggle]');
      if (toggle && form.contains(toggle)) {
        e.preventDefault();
        const id = toggle.getAttribute('data-toggle');
        const panel = id ? document.getElementById(id) : null;
        if (panel) {
          const open = panel.style.display !== 'none';
          panel.style.display = open ? 'none' : 'block';
          toggle.textContent = open ? 'What we’re looking for (examples)' : 'Hide details';
        }
        return;
      }

      // Select answer
      const btn = e.target.closest('button[data-v]');
      if (!btn || !form.contains(btn)) return;

      const field = btn.closest('.field');
      if (!field) return;

      const id = field.dataset.id;
      const val = Number(btn.dataset.v);
      state[id] = val;

      field.querySelectorAll('button[data-v]').forEach(b => b.classList.remove('active'));
      btn.classList.add('active');

      refreshGroupCounters();
      update();
    });

    resetBtn?.addEventListener('click', ()=>{
      Object.keys(state).forEach(k => state[k] = null);
      form?.querySelectorAll('button[data-v].active').forEach(b => b.classList.remove('active'));
      form?.querySelectorAll('.explain').forEach(el => { el.style.display = 'none'; });
      form?.querySelectorAll('[data-toggle]').forEach(a => { a.textContent = 'What we’re looking for (examples)'; });

      refreshGroupCounters();
      update();
    });

    pdfBtn?.addEventListener('click', ()=>{
      const { answered } = compute(state);
      if (answered < QUESTIONS.length) { alert('Please answer all questions first.'); return; }
      openModal();
    });

    function openModal(){ if(modal){ modal.style.display = 'flex'; nameEl?.focus(); } }
    function closeModal(){ if(modal){ modal.style.display = 'none'; } }
    cancelLead?.addEventListener('click', closeModal);
    modal?.addEventListener('click', (e)=>{ if(e.target === modal) closeModal(); });
    document.addEventListener('keydown', (e)=>{ if(e.key === 'Escape') closeModal(); });

    leadForm?.addEventListener('submit', async (e) => {
      e.preventDefault();

      const name  = (nameEl?.value || '').trim();
      const email = (emailEl?.value || '').trim();

      if (!name || !/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email)) {
        alert('Please enter your name and a valid email.');
        return;
      }

      const payload = {
        name,
        email,
        subject: `Checklist lead: ${STANDARD}`,
        message: JSON.stringify({
          checklist: STANDARD,
          checklistId: 'iso27701',
          timestamp: new Date().toISOString(),
          ua: navigator.userAgent,
          ...compute(state),
          total: QUESTIONS.length,
          answers: state,
          questions: QUESTIONS.map(q => ({ id:q.id, title:q.title, group:q.group })),
          groups: GROUPS.map(g => ({ key:g.key, title:g.title }))
        })
      };

      try {
        const res = await fetch(API_URL, {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify(payload)
        });
        if (!res.ok) alert('We could not notify our team automatically, but your PDF will still download.');
      } catch {
        alert('We could not notify our team automatically, but your PDF will still download.');
      }

      try {
        const s = compute(state);
        if (window.gtag) {
          window.gtag('event', 'checklist_submit', {
            event_category: 'Checklist',
            event_label: STANDARD,
            checklist_standard: STANDARD,
            checklist_id: 'iso27701',
            answered: s.answered,
            score: s.score
          });
        }
      } catch {}

      closeModal();
      generatePDF(state, { name, email });
    });

    refreshGroupCounters();
    update();

    function update(){
      const { answered, score } = compute(state);
      const pct = Math.round((answered / QUESTIONS.length) * 100);

      if (bar) bar.style.width = pct + '%';
      if (scoreNum) scoreNum.textContent = `${score}%`;

      let label='Not started', hint='Answer the questions to see your readiness.';
      if (answered === 0) {
        label = 'Not started';
        hint  = 'Answer the questions to see your readiness.';
      } else if (score <= 30) {
        label = 'High Risk (0–30)';
        hint  = 'Start with ownership, data register, request handling, vendor contracts, and evidence hygiene.';
      } else if (score <= 60) {
        label = 'Partially Ready (31–60)';
        hint  = 'Prioritise consistency: close foundational gaps and improve evidence so you can answer audits quickly.';
      } else if (score <= 85) {
        label = 'Nearly Audit-Ready (61–85)';
        hint  = 'Close the remaining “Partial/No” items and package evidence for an audit-ready narrative.';
      } else {
        label = 'Certification-Ready (86–100)';
        hint  = 'Focus on validation, evidence freshness and audit preparation.';
      }

      if (scoreLabel) scoreLabel.textContent = label;
      if (scoreHint)  scoreHint.textContent  = hint;
    }
  }

  function compute(state){
    const answered = Object.values(state).filter(v => v !== null).length;
    const scoreRaw = Object.values(state).reduce((a,v)=> a + (v ?? 0), 0);
    const score    = Math.round((scoreRaw / (Object.keys(state).length * 2)) * 100);
    return { answered, score: isNaN(score) ? 0 : score };
  }

  // ===== PDF generation unchanged from your prior approach (kept minimal here) =====
  function getBand(score, answered){
    if (!answered) return { key:'na', title:'Not started', desc:'Complete the checklist to generate your Trust Score.' };
    if (score <= 30) return { key:'hr',  title:'0–30 — High Risk',             desc:'Missing privacy foundations. Structured remediation required.' };
    if (score <= 60) return { key:'pr',  title:'31–60 — Partially Ready',      desc:'Core elements exist. Prioritisation and closure of key gaps needed.' };
    if (score <= 85) return { key:'nar', title:'61–85 — Nearly Audit-Ready',   desc:'Close minor gaps and package evidence for audit.' };
    return              { key:'cr',      title:'86–100 — Certification-Ready', desc:'Controls largely in place. Focus on validation and audit packaging.' };
  }

  function generatePDF(state, lead = {}) {
    const { jsPDF } = window.jspdf || {};
    if(!jsPDF){ alert('PDF library not loaded.'); return; }

    // Your existing PDF logic can stay; if you want, paste your full PDF body here.
    // This stub keeps your page functional while you bring over your existing PDF block.
    const doc = new jsPDF('p','mm','a4');
    doc.setFont('helvetica','bold'); doc.setFontSize(16);
    doc.text('Compliance365 — Readiness Report', 14, 20);
    doc.setFont('helvetica','normal'); doc.setFontSize(11);
    doc.text(`${STANDARD} quick-scan summary`, 14, 28);

    const vals      = Object.values(state);
    const answered  = vals.filter(v => v !== null).length;
    const scoreRaw  = vals.reduce((a,v)=> a + (v ?? 0), 0);
    const score     = Math.round((scoreRaw / (vals.length * 2)) * 100) || 0;
    const band      = getBand(score, answered);

    doc.text(`Prepared for: ${(lead?.name||'')} ${(lead?.email||'')}`.trim(), 14, 40);
    doc.text(`Score: ${score}%`, 14, 48);
    doc.text(`Status: ${band.title}`, 14, 56);

    doc.save('iso27701_readiness.pdf');
  }
})();
  </script>

  <style>
    .info-cards{ display:grid; grid-template-columns:repeat(auto-fit,minmax(220px,1fr)); gap:16px; margin-top:20px; }
    .card{ padding:16px; border:1px solid rgba(10,18,32,.10); border-radius:14px; background:#fff; }
    .card.soft{ background:#f9fafc; border-color:#e4e8f0; }

    .tabs{ margin:12px 0 8px; display:flex; gap:10px; flex-wrap:wrap; }

    .progress{ height:8px; background:#eef2ff; border-radius:999px; overflow:hidden; margin:10px 0 16px; }
    .progress-bar{ height:100%; width:0%; background:linear-gradient(90deg,#4F46E5,#06B6D4); transition:width .2s; }

    .form-grid{ display:grid; gap:18px; margin-top:12px; }
    .groupHead{ display:flex; align-items:flex-start; justify-content:space-between; gap:10px; }
    .group-fields{ margin-top:10px; display:grid; gap:12px; }

    .field{ padding:14px; border:1px solid rgba(10,18,32,.08); border-radius:14px; background:#fff; }
    .qTitle{ display:block; font-weight:900; margin:0 0 6px; }
    .qMeta{ display:flex; align-items:center; gap:10px; flex-wrap:wrap; margin:0 0 10px; }

    .detailsToggle{ color:#4F46E5; font-weight:800; text-decoration:underline; }
    .chip{
      display:inline-flex; align-items:center;
      padding:2px 8px; border-radius:999px;
      border:1px solid rgba(10,18,32,.12);
      background:#f9fafc;
      font-size:12px; font-weight:700;
      margin-right:6px;
    }

    .explain{ margin-top:10px; padding:12px; border:1px solid rgba(10,18,32,.08); border-radius:12px; background:#f9fafc; }

    .opts{ display:flex; gap:8px; flex-wrap:wrap; }

    /* Dedicated answer button styles (no collision with `.pill`) */
    .optBtn{
      appearance:none;
      border:1px solid rgba(10,18,32,.14);
      background:#fff;
      border-radius:999px;
      padding:8px 12px;
      font-weight:800;
      cursor:pointer;
      user-select:none;
    }
    .optBtn:hover{ border-color: rgba(79,70,229,.45); }
    .optBtn.active{
      background:#f0f2ff;
      border-color:#4F46E5;
      color:#1f2a5c;
    }

    .actions{ display:flex; justify-content:space-between; align-items:center; margin-top:16px; gap:10px; }

    .stickyScore{
      position:sticky; top:10px; z-index:5;
      background:rgba(255,255,255,.92);
      backdrop-filter: blur(6px);
      border:1px solid rgba(10,18,32,.08);
      border-radius:14px;
      padding:12px 14px;
      margin-bottom:14px;
      display:flex;
      align-items:center;
      justify-content:space-between;
      gap:12px;
    }
    .stickyTitle{ font-weight:900; }
    .stickyRight{ display:flex; align-items:center; gap:12px; }
    .scoreNum{ font-weight:900; font-size:1.6rem; min-width:64px; text-align:right; }

    .glossaryGrid{ display:grid; grid-template-columns:repeat(auto-fit,minmax(260px,1fr)); gap:10px; margin-top:10px; }
    .glossaryItem{ display:flex; gap:10px; padding:10px; border:1px solid rgba(10,18,32,.08); border-radius:12px; background:#fff; }
    .glossKey{
      min-width:64px; font-weight:900;
      color:#1f2a5c; background:#eef2ff;
      border:1px solid rgba(79,70,229,.2);
      border-radius:10px; padding:6px 10px;
      height:fit-content; text-align:center;
    }
    .glossTerm{ font-weight:800; margin-bottom:2px; }
  </style>

  <!-- Lead capture modal (kept same) -->
  <div id="leadModal"
       style="display:none; position:fixed; inset:0; background:rgba(0,0,0,.5); align-items:center; justify-content:center; z-index:2000;">
    <div style="background:#fff; padding:24px; border-radius:14px; max-width:420px; width:100%; box-shadow:0 12px 30px rgba(0,0,0,.2);">
      <h3 style="margin-top:0">Before you download</h3>
      <p class="muted small">
        Enter your details to receive this ISO 27701 readiness report and occasional privacy, security and AI governance updates:
      </p>
      <form id="leadForm">
        <input type="text" id="name" placeholder="Your name" style="width:100%; margin:6px 0; padding:10px; border:1px solid #ccc; border-radius:8px">
        <input type="email" id="email" placeholder="Your email" style="width:100%; margin:6px 0; padding:10px; border:1px solid #ccc; border-radius:8px">

        <p class="muted tiny" style="margin-top:6px">
          By submitting, you agree to receive relevant updates from Compliance365. You can unsubscribe at any time.
        </p>

        <div style="display:flex; gap:10px; margin-top:12px; justify-content:flex-end;">
          <button type="button" class="btn btn-ghost" id="cancelLead">Cancel</button>
          <button type="submit" class="btn btn-primary">Email my PDF report</button>
        </div>
      </form>
    </div>
  </div>
</Base>
